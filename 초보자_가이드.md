# 🎓 네이버 블로그 포스트 생성기 - 초보자 가이드

## 📚 목차
1. [프로그램 준비하기](#1-프로그램-준비하기)
2. [제품 정보 입력하기](#2-제품-정보-입력하기)
3. [블로그 글 만들기](#3-블로그-글-만들기)
4. [결과 확인하기](#4-결과-확인하기)

---

## 1. 프로그램 준비하기

### 📥 Python 설치하기

**Windows 사용자:**
1. 구글에서 "Python 다운로드" 검색
2. python.org 사이트에서 Python 다운로드 (3.7 이상)
3. 다운로드한 파일 실행
4. ⚠️ **중요**: "Add Python to PATH" 체크박스 꼭 선택!
5. "Install Now" 클릭

**Mac 사용자:**
1. 터미널 열기 (Spotlight에서 "터미널" 검색)
2. Python이 이미 설치되어 있을 수 있습니다
3. 확인 방법: `python3 --version` 입력

### 📂 프로그램 파일 준비하기

이 프로그램은 다음 파일들로 구성되어 있습니다:
- `naver_blog_generator.py` - 프로그램 본체
- `generate_post.py` - 실행 파일
- `sample_product.json` - 예시 제품 정보

---

## 2. 제품 정보 입력하기

### 📝 제품 정보 파일 만들기

제품 정보는 `sample_product.json` 파일을 수정해서 만듭니다.

#### 🔍 JSON 파일이란?
- 컴퓨터가 쉽게 읽을 수 있는 메모장 파일입니다
- Windows 메모장이나 Mac 텍스트 편집기로 열 수 있어요

#### ✏️ 파일 수정 방법

**1단계: 파일 열기**
- Windows: 파일 우클릭 → "연결 프로그램" → "메모장"
- Mac: 파일 우클릭 → "다음으로 열기" → "텍스트 편집"

**2단계: 내용 수정하기**

```json
{
  "name": "여기에 제품 이름을 입력하세요",
  "price": 100000,
  "brand": "브랜드 이름",
  "category": "카테고리",
  "rating": 4.5,
  "description": "제품에 대한 간단한 설명",
  "images": [
    "이미지 URL 1",
    "이미지 URL 2",
    "이미지 URL 3"
  ],
  "features": [
    "특징 1",
    "특징 2",
    "특징 3"
  ],
  "pros": [
    "장점 1",
    "장점 2",
    "장점 3"
  ],
  "cons": [
    "단점 1",
    "단점 2"
  ]
}
```

#### 📋 각 항목 설명

| 항목 | 설명 | 예시 |
|------|------|------|
| `name` | 제품 이름 | "아이폰 15 프로" |
| `price` | 가격 (숫자만, 쉼표 없이) | 1500000 |
| `brand` | 브랜드 | "애플" |
| `category` | 카테고리 | "스마트폰" |
| `rating` | 별점 (1~5) | 4.5 |
| `description` | 간단한 설명 | "최신 아이폰" |
| `images` | 이미지 주소들 | 인터넷 이미지 URL |
| `features` | 특징들 | "강력한 성능", "좋은 카메라" |
| `pros` | 장점들 | "빠른 속도", "예쁜 디자인" |
| `cons` | 단점들 | "비싼 가격", "무거움" |

#### ⚠️ 주의사항

1. **따옴표 꼭 지키기**: 글자는 `"` 안에 넣어야 해요
2. **쉼표 빠뜨리지 않기**: 줄 끝에 `,` 있는지 확인 (마지막 줄 제외)
3. **숫자는 따옴표 없이**: `price`처럼 숫자는 `"` 없이 써요
4. **한글 사용 가능**: 모든 내용을 한글로 써도 됩니다!

#### 💡 실제 예시

```json
{
  "name": "다이슨 에어랩",
  "price": 699000,
  "brand": "다이슨",
  "category": "헤어드라이기",
  "rating": 4.8,
  "description": "머리를 말리면서 스타일링까지!",
  "images": [
    "https://example.com/image1.jpg",
    "https://example.com/image2.jpg"
  ],
  "features": [
    "열 손상 방지",
    "빠른 건조",
    "다양한 스타일링"
  ],
  "pros": [
    "머리 손상이 적음",
    "스타일링이 쉬움",
    "고급스러운 디자인"
  ],
  "cons": [
    "가격이 비쌈",
    "무게가 있음"
  ]
}
```

**3단계: 파일 저장하기**
- 파일 이름: `my_product.json` (원하는 이름으로)
- 저장 위치: 프로그램 파일들과 같은 폴더

---

## 3. 블로그 글 만들기

### 🖥️ 프로그램 실행하기

#### Windows 사용자

**1단계: 명령 프롬프트 열기**
- 프로그램 폴더에서 주소창 클릭
- `cmd` 입력하고 엔터
- 검은 화면이 나타납니다

**2단계: 명령어 입력**

아래 명령어를 복사해서 붙여넣기 하세요:

```
python generate_post.py --product-file my_product.json --template review --output 블로그글.html
```

**3단계: 엔터 누르기**
- "✅ 블로그 포스트가 '블로그글.html' 파일로 저장되었습니다." 메시지가 보이면 성공!

#### Mac/Linux 사용자

**1단계: 터미널 열기**
- Spotlight에서 "터미널" 검색
- 프로그램 폴더로 이동: `cd 폴더경로`

**2단계: 명령어 입력**

```bash
python3 generate_post.py --product-file my_product.json --template review --output 블로그글.html
```

### 🎨 다양한 템플릿 선택하기

`--template` 뒤의 단어를 바꾸면 다른 스타일의 글이 만들어져요:

| 템플릿 | 설명 | 언제 사용? |
|--------|------|------------|
| `review` | 리뷰 형식 | 제품 사용 후기를 쓸 때 |
| `intro` | 소개 형식 | 제품을 처음 소개할 때 |
| `comparison` | 비교 형식 | 장단점을 비교할 때 |
| `unboxing` | 언박싱 형식 | 개봉기를 쓸 때 |

#### 예시 명령어

**리뷰 글 만들기:**
```
python generate_post.py --product-file my_product.json --template review --output 리뷰.html
```

**언박싱 글 만들기:**
```
python generate_post.py --product-file my_product.json --template unboxing --output 언박싱.html
```

**마크다운 형식으로 만들기:**
```
python generate_post.py --product-file my_product.json --template review --format markdown --output 블로그글.md
```

---

## 4. 결과 확인하기

### 📄 HTML 파일 열기

**1단계: 파일 찾기**
- 프로그램 폴더에서 `블로그글.html` 파일 찾기

**2단계: 브라우저로 열기**
- 파일을 더블클릭하거나
- 파일을 크롬/엣지/사파리 브라우저로 드래그

**3단계: 결과 확인**
- 예쁘게 꾸며진 블로그 글이 보입니다!
- 제목, 이미지, 특징, 장단점 등이 정리되어 있어요

### 📋 마크다운 파일 열기

- 메모장이나 텍스트 편집기로 열기
- 네이버 블로그 작성 화면에 복사-붙여넣기

### ✂️ 네이버 블로그에 올리기

**1단계: HTML 파일 열기**
- 브라우저로 열린 블로그 글 전체 선택 (Ctrl+A 또는 Command+A)

**2단계: 복사하기**
- Ctrl+C (또는 Command+C)

**3단계: 네이버 블로그 붙여넣기**
- 네이버 블로그 글쓰기 화면에서 Ctrl+V (또는 Command+V)
- 필요하면 수정하기
- 발행!

---

## 🆘 문제 해결

### ❌ "python을 찾을 수 없습니다" 에러

**해결 방법:**
- Windows: `python` 대신 `python3` 사용
- Python이 제대로 설치되지 않았을 수 있어요
- Python 다시 설치 (Add to PATH 꼭 체크!)

### ❌ "파일을 찾을 수 없습니다" 에러

**해결 방법:**
- 파일 이름 다시 확인
- 파일이 같은 폴더에 있는지 확인
- 파일 이름에 띄어쓰기가 있으면 따옴표로 감싸기:
  ```
  python generate_post.py --product-file "내 제품.json" --template review --output 결과.html
  ```

### ❌ "JSON 파싱 실패" 에러

**해결 방법:**
- JSON 파일 문법 확인
- 쉼표(,), 따옴표("), 중괄호({}) 제대로 있는지 확인
- 온라인 JSON 검사 도구 사용: jsonlint.com

### ❌ 한글이 깨져 보여요

**해결 방법:**
- 파일을 UTF-8 인코딩으로 저장
- 메모장: "다른 이름으로 저장" → 인코딩 "UTF-8" 선택

---

## 💡 꿀팁

### 🚀 빠르게 시작하기

샘플 파일로 먼저 연습해보세요:

```
python generate_post.py --product-file sample_product.json --template review --output test.html
```

### 📸 이미지 주소 찾기

1. 구글 이미지 검색
2. 원하는 이미지 우클릭
3. "이미지 주소 복사"
4. JSON 파일의 `images`에 붙여넣기

### 🎯 여러 개 한꺼번에 만들기

4가지 템플릿 모두 만들기:

```bash
python generate_post.py --product-file my_product.json --template review --output 리뷰.html
python generate_post.py --product-file my_product.json --template intro --output 소개.html
python generate_post.py --product-file my_product.json --template comparison --output 비교.html
python generate_post.py --product-file my_product.json --template unboxing --output 언박싱.html
```

### 📝 JSON 파일 쉽게 만들기

1. `sample_product.json` 복사
2. 이름 바꾸기 (예: `my_product.json`)
3. 내용만 수정하기

---

## 🎉 완성!

이제 여러분도 전문가처럼 블로그 글을 만들 수 있습니다!

### 다음 단계

1. ✅ 샘플 파일로 연습하기
2. ✅ 내 제품 정보 입력하기
3. ✅ 다양한 템플릿 시도하기
4. ✅ 네이버 블로그에 올리기

### 추가 도움이 필요하면?

- README.md 파일 읽어보기
- example_usage.py 실행해보기
- 에러 메시지 구글에서 검색하기

**화이팅! 🎊**
