# 🚀 키워드 추천 시스템 실행 방법

## 📋 1단계: 패키지 설치

터미널(명령 프롬프트)을 열고 다음 명령어를 실행하세요:

```bash
pip install streamlit pandas requests
```

또는 requirements 파일 사용:

```bash
pip install -r requirements_keyword_finder.txt
```

---

## 🎯 2단계: 앱 실행

같은 터미널에서 다음 명령어를 실행하세요:

```bash
streamlit run keyword_finder_improved.py
```

---

## 🌐 3단계: 브라우저에서 확인

명령어를 실행하면 자동으로 브라우저가 열립니다!

보통 이렇게 표시됩니다:
```
You can now view your Streamlit app in your browser.

  Local URL: http://localhost:8501
  Network URL: http://192.168.x.x:8501
```

자동으로 안 열리면 브라우저에서 **http://localhost:8501** 을 입력하세요.

---

## 🎮 4단계: 사용하기

1. 검색어 입력 (예: "무선이어폰", "로봇청소기")
2. 사이드바에서 설정 조정
   - 구매확정 키워드 개수
   - 정보성 키워드 개수
   - 최소 검색량
   - 필터 강도
3. "🚀 최적 키워드 자동 추천" 버튼 클릭
4. 결과 확인 및 CSV 다운로드

---

## ⚠️ 문제 해결

### "streamlit: command not found" 에러가 나면?
```bash
python -m streamlit run keyword_finder_improved.py
```

### 포트가 이미 사용 중이라면?
```bash
streamlit run keyword_finder_improved.py --server.port 8502
```

### 패키지 설치 권한 에러가 나면?
```bash
pip install --user streamlit pandas requests
```

---

## 💡 팁

- **중단하기**: 터미널에서 `Ctrl + C` 누르기
- **재실행**: 코드 수정 후 브라우저에서 "Always rerun" 클릭
- **포트 변경**: `--server.port 포트번호` 옵션 사용

---

## 📞 도움이 필요하면?

에러 메시지를 복사해서 알려주세요!
