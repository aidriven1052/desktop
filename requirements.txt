# 네이버 블로그 포스트 생성기 필수 패키지
# Python 3.7 이상 필요

# 기본적으로 Python 표준 라이브러리만 사용하므로 추가 패키지가 필요 없습니다.
# 하지만 향후 확장을 위해 유용한 패키지들을 나열합니다:

# 마크다운을 HTML로 변환 (선택사항)
# markdown>=3.4.0

# 이미지 처리 (선택사항)
# Pillow>=10.0.0

# HTTP 요청 (이미지 다운로드 등, 선택사항)
# requests>=2.31.0

# 더 나은 CLI 인터페이스 (선택사항)
# rich>=13.0.0

# OpenAI API를 사용한 AI 글 생성 (선택사항)
# openai>=1.0.0
